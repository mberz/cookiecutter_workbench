name: {{cookiecutter.project_slug}}
channels:
  - defaults
dependencies:
  - pip
  - pyflakes
  - python={{cookiecutter.python_version}}
  - scons
  - flake8
  - numpy
  - scipy
  - matplotlib
  - ipython
  - ipykernel
  - sympy
  - pip:
    - -e ./utils/
prefix: ./env/{{cookiecutter.project_slug}}